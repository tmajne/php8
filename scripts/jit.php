<?php

/*
 * Testy syntetyczne służą do pomiarów pojedynczych komponentów systemu,
 * dają nam informacje o pomiarze jakiejś składowej niezależnie od pozostałych 
 * aspektów.
 *
 * V8 engine
 *
 * Applications that are IO-bound, such as the ones that extensively 
 * use a database, DNS queries, file read/write operations, FTP, sockets, 
 * etc. might not see a noticeable difference because more often than not,
 * the IO operations are themselves the bottleneck of such application.
 */
